## 1. 状态模型扩展

- [x] 1.1 在 state 中增加 mediaType 字段（'video' | 'photo'）
- [x] 1.2 增加 photoFile、photoUrl 状态字段
- [x] 1.3 增加对应的 fileInput（accept="image/*"）用于照片选取

## 2. 素材选取界面改造

- [x] 2.1 重构第一界面 UI：添加两个选取按钮（"选取视频"、"选取照片"）
- [x] 2.2 实现照片选取回调：读取照片文件，设置 mediaType 和 photoUrl
- [x] 2.3 照片选取后获取图片元数据（宽高），自动计算预览适配

## 3. 导航流程适配

- [x] 3.1 步骤指示器根据 mediaType 动态显示（照片模式隐藏步骤 2 的圆点）
- [x] 3.2 照片模式下"下一步"直接跳转到文字编辑（跳过裁剪步骤）
- [x] 3.3 "上一步"按钮根据 mediaType 正确返回上一步（照片模式从步骤 2 返回步骤 0）

## 4. 照片预览适配

- [x] 4.1 文字编辑页面（renderStep2）根据 mediaType 渲染 img 或 video
- [x] 4.2 文字拖拽逻辑在照片模式下正常工作
- [x] 4.3 导出预览页面（renderStep3）根据 mediaType 渲染对应预览

## 5. 图片导出功能

- [x] 5.1 实现照片+文字 Canvas 合成函数（加载照片→绘制到Canvas→叠加文字）
- [x] 5.2 实现 PNG 导出（canvas.toBlob → 触发下载）
- [x] 5.3 导出按钮文案根据 mediaType 变化（"导出 GIF" / "导出图片"）
- [x] 5.4 导出成功后在预览区展示合成后的图片（使用 img 标签）

## 6. 兼容性与清理

- [x] 6.1 切换素材类型时清空旧状态（从视频切照片时清除视频相关字段，反之亦然）
- [x] 6.2 确保视频原有流程不受影响（回归测试）
